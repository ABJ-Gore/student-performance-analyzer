# Student Exam Performance Analyzer

Interactive Python program that analyzes the Kaggle “Students Performance in Exams” dataset.
Users can view stats, compare groups (gender/lunch/test prep/etc.), and plot score distributions through a terminal menu.

## How to run
1. Install requirements:
   ```bash
   pip install pandas matplotlib
